#include <stdio.h>

int dilma(int a){
while(scanf("%d",&a)==1){
    if(a==0)
        printf("vai ter copa!\n");
    else
        printf("vai ter duas!\n");
    }
}
int main() {
int N;
dilma(N);
    return 0;
}
